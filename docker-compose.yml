services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - TZ=Europe/Kyiv
    ports:
      - "${OAUTH_PORT:-8080}:${OAUTH_PORT:-8080}"
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
